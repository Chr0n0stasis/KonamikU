[package]
name    = "pmm-rust"
version = "0.1.0"
edition = "2021"

[lib]
name      = "pmm_rust"
crate-type = ["staticlib"]   # 编译为静态库供 C 链接

[profile.release]
opt-level = "z"    # 体积优化
lto       = true
panic     = "abort"